networks:
  kelder_net:
    name: kelder_net
    external: true

services:
  redis:
    image: redis:alpine
    container_name: redis
    ports:
      - "6379:6379"
    deploy:
      resources:
        limits:
          memory: 128M
    networks:
      - kelder_net
    #restart: "on-failure"

  kelder_api:
    build: .
    container_name: kelder_api
    privileged: true
    tty: true
    volumes:
      - /dev:/dev
      - ${HOST_PROJECT_ROOT:-.}/logs:/app/logs
      - ${HOST_PROJECT_ROOT:-.}/journey_history:/app/src/kelder_api/assets/
    devices:
      - /dev/gpiomem
      - /dev/mem
    cap_add:
      - SYS_RAWIO
    ports:
      - "8000:80"
    depends_on:
      - redis
    environment:
      - REDIS_HOST=redis
    env_file:
      - .env
    extra_hosts:
      - "host.docker.internal:host-gateway"
    networks:
      - kelder_net
    # restart: "on-failure"

  worker:
    build: .
    privileged: true
    volumes:
      - ${HOST_PROJECT_ROOT:-.}/logs:/app/logs
      - ${HOST_PROJECT_ROOT:-.}/journey_history:/app/src/kelder_api/assets/
    environment:
      REDIS_HOST: redis
    devices:
      - "/dev/ttyAMA0:/dev/ttyAMA0"
      - "/dev/ttyS0:/dev/ttyS0"
    command: [ "uv", "run", "python", "-m", "src.kelder_api.app.background_orchestrator" ]
    depends_on:
      - redis
    env_file:
      - .env
    networks:
      - kelder_net
    #restart: "on-failure"
